<Page
    x:Class="Client.Views.LobbyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:c="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    xmlns:pswrd="using:Client.Behaviors"
    xmlns:w="http://schemas.microsoft.com/expression/2010/interactivity"

    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding Source={StaticResource Locator}, Path=LobbyVM}">
    <Viewbox Stretch="Fill">
        <Grid>
            <i:Interaction.Behaviors>
                <c:EventTriggerBehavior EventName="Loaded">
                    <c:InvokeCommandAction  Command="{Binding GetFiles, Mode=TwoWay}"></c:InvokeCommandAction>
                </c:EventTriggerBehavior>
            </i:Interaction.Behaviors>


            <ListView x:ConnectionId='2' Name="listofFiles" ItemsSource="{Binding Files}" Height="400" VerticalAlignment="Center" HorizontalAlignment="Stretch">
                <i:Interaction.Behaviors>
                    <c:EventTriggerBehavior EventName="SelectionChanged">
                        <c:InvokeCommandAction Command="{Binding SelectedFileCommand}" CommandParameter="{Binding ElementName=listofFiles, Path=SelectedItem}"></c:InvokeCommandAction>
                    </c:EventTriggerBehavior>
                </i:Interaction.Behaviors>
                <ListView.HeaderTemplate>
                    <DataTemplate>
                        <Grid Padding="15" Background="{ThemeResource SystemBaseLowColor}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="350" />
                                <ColumnDefinition Width="350" />
                                <ColumnDefinition Width="350"/>
                                <ColumnDefinition Width="350"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"  HorizontalAlignment="Center" Text="File Name" FontSize="20" FontWeight="Bold" FontFamily="Thomba" Foreground="Black"/>
                            <TextBlock Grid.Column="1"  HorizontalAlignment="Center" Text="Description" FontSize="20" FontWeight="Bold" FontFamily="Thomba" Foreground="Black"/>
                            <TextBlock Grid.Column="2"   HorizontalAlignment="Center" Text="File Type"  FontSize="20" FontWeight="Bold" FontFamily="Thomba" Foreground="Black"/>
                            <TextBlock Grid.Column="3"  HorizontalAlignment="Center" Text="Date Uploaded" FontSize="20" FontWeight="Bold" FontFamily="Thomba" Foreground="Black"/>
                        </Grid>
                    </DataTemplate>
                </ListView.HeaderTemplate>
                <ListView.ItemTemplate>
                    <DataTemplate >
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="350"/>
                                <ColumnDefinition Width="350"/>
                                <ColumnDefinition Width="350"/>
                                <ColumnDefinition Width="350"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock  Grid.Column="0" VerticalAlignment="Center"  HorizontalAlignment="Center" Text="{Binding FileName}" FontSize="25" FontWeight="Bold" FontFamily="Segoe WP Black"   ></TextBlock>
                            <TextBlock  Grid.Column="1"  VerticalAlignment="Center"  HorizontalAlignment="Center" Text="{Binding Description}" FontSize="25" FontWeight="Bold" FontFamily="Segoe WP Black"></TextBlock>
                            <TextBlock Grid.Column="2"  VerticalAlignment="Center"  HorizontalAlignment="Center" Text="{Binding FileEnding}" FontSize="25" FontWeight="Bold" FontFamily="Segoe WP Black"></TextBlock>
                            <TextBlock  Grid.Column="3"  VerticalAlignment="Center"  HorizontalAlignment="Center" Text="{Binding DateUploaded}" FontSize="25" FontWeight="Bold" FontFamily="Segoe WP Black"></TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
            <Button  Content="Upload File"  Command="{Binding GoToUploadFileView}"  FontFamily="Thomba" HorizontalAlignment="Right" VerticalAlignment="Bottom" FontSize="25" Width="300" Height="36" Margin="0,0,42,14" />
            <Button  Content="Sign Out"  Command="{Binding SignOutCommand}" FontFamily="Thomba" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontSize="25" Width="300" Height="37" Margin="42,0,0,14" />
            <Button  Content="Download File"  Command="{Binding DownloadFileCommand}" FontFamily="Thomba" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontSize="25" Width="300" Height="37" Margin="388,0,0,14" />
            <Button  Content="Delete File"  Command="{Binding DeleteFileCommand}" FontFamily="Thomba" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontSize="25" Width="300" Height="37" Margin="715,0,0,14" />

            <Grid.Background>
                <ImageBrush ImageSource="/Assets/mainbg.jpg"></ImageBrush>
            </Grid.Background>

        </Grid>
    </Viewbox>

</Page>

